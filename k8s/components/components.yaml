network:
  release: "ck-network"
  chart: "cilium-1.14.1.tgz"
  namespace: "kube-system"
  images:
    cilium-agent:
      repository: "ghcr.io/canonical/cilium"
      tag: "1.14.5-ck1"
    cilium-operator:
      repository: "ghcr.io/canonical/cilium-operator"
      tag: "1.14.5-ck2"
dns:
  release: "ck-dns"
  chart: "coredns-1.29.0.tgz"
  namespace: "kube-system"
  # TODO: Add after deciding on how adjust manifests args & entrypoints
  # images:
  #   coredns:
  #     repository: "ghcr.io/canonical/coredns"
  #     tag: "1.11.1-ck1"
storage:
  release: "ck-storage"
  chart: "hostpath-provisioner-0.1.0.tgz"
  namespace: "kube-system"
ingress:
  parent: "network"
gateway:
  release: "ck-gateway"
  chart: "gateway-api-0.7.1.tgz"
  namespace: "kube-system"
loadbalancer:
  release: "ck-loadbalancer"
  chart: "ck-loadbalancer"
  namespace: "kube-system"
