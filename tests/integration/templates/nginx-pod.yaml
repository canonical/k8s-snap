apiVersion: v1
kind: Pod
metadata:
  labels:
    app: nginx
  name: nginx
  namespace: default
spec:
  containers:
    - name: nginx
      image: ghcr.io/containerd/nginx:1.27.0
  restartPolicy: Always
